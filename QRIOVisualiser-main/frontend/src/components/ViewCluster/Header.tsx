import React from "react";

const Header = () => {
  return (
    <div style={{ fontFamily: "Poppins-ExtraBold", fontSize: "40px" }}>
      Qube Cluster
    </div>
  );
};

export default React.memo(Header);
